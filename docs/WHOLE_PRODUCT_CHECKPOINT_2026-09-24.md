# Whole-product checkpoint — September 24, 2026

## Decision

The model-led brain is credible enough to begin integrating into Growth Operator as a **bounded research-backed input while it continues to improve**. It is not credible enough to act as an autonomous source of proven revenue, conversion or margin conclusions. The claim ledger is the safety boundary: product-scoped source pages and exact quotes can support a visible investigation or leverage recommendation; missing provenance must remain an investigation.

This means the Investigation Lab should not become an open-ended hardening project. The next work should connect its judgment to the existing Snapshot / opportunity / Mission loop.

## What still prevents an unqualified action recommendation

The remaining intelligence weakness is not search coverage. It is the gap between public-source truth and operator economics: GO cannot infer demand, capacity, conversion, margin, ownership or causal impact from a page and a quote. A product claim can be well-sourced and still require a click test, booking data, CRM data or operator confirmation. The current action-plan bridge therefore preserves the claim, evidence IDs, scope and proof requirement, but downgrades unsupported action claims to `INVESTIGATE`.

## What can improve through real product use

Connected booking and analytics data can establish funnel movement; CRM and capacity data can establish economics; customer/operator feedback can improve prioritization and language; mission outcomes can teach GO which recommendations are executable. These are product-learning loops, not reasons to buy more SerpApi searches now. Archived evidence and replay remain sufficient for provenance and regression work.

## Shortest path to the complete GO loop

1. **Understand / Investigate / Judge:** run the bounded research adapter and persist the claim ledger.
2. **Present:** show one primary claim in the existing Snapshot as a research-backed current read, with exact quote, source page, confidence and the proof still needed.
3. **Create a Mission:** map the claim through the action-plan bridge into the existing mission checklist; only validated-provenance claims may appear as action candidates, while others create bounded investigation missions.
4. **Execute / Measure:** use the current bounded checklist and capture baseline, after-state and operator notes in the existing mission completion path; add connectors when available.
5. **Remember / Next opportunity:** persist the claim ID, evidence IDs, mission result and unresolved proof requirement so the next investigation starts from what was learned.

The first integration seam is now implemented: a completed `PROOF_JUDGED` Investigation Lab result can be saved as `growthOperatorResearchJudgment`, opened in the real dashboard, rendered as the source-backed current read, and carried into the existing Mission checklist. The dashboard keeps the claim ID, evidence scope and proof requirement in the finding model and does not fabricate revenue when connected economics are absent. A browser-facing regression covers the handoff and action boundary.

Tomorrow's founder test should use the dashboard experience, not the Lab developer page: run or load a real operator read, inspect the business understanding/current read, open the primary finding, and start the bounded mission. The test should judge whether the operator feels understood and whether the next move is commercially meaningful—not whether the internal evidence machinery is interesting. SerpApi remains exhausted and is intentionally not a blocker for this integration seam; a current live run still requires quota, while archived replay remains valid for the evidence contract.

### Readiness correction

The initial dashboard bridge was wired but **not founder ready**. A subsequent review found that the claim ledger read non-existent model fields (`why` / `action` instead of `whyItMatters` / `actionBoundary`) and ranked by action type rather than the model's chosen `nextMove`. This lost the actual commercial reasoning and selected action. The corrected bridge now uses the real model schema, anchors the chosen finding, and passes a regression with that schema plus an archived First Lady replay. The dashboard and Mission still contain synthetic preview scores, bookings, revenue, comparison graphics and generic tasks. Those need a research-mode presentation and a real end-to-end browser check before an operator should judge the experience. A fresh partner run also needs working search quota and an accessible product runtime. Do not call the current UI founder-test ready merely because the adapter passes local regression.

Research mode now presents the chosen move, source quotes/pages and proof requirement in the dashboard; it hides unrelated synthetic workbench, scores, KPIs and progress. Its Mission uses claim-specific verification, baseline and approval-preparation steps and records `RESEARCH_PREPARED`, with no assertion that GO published work or measured impact. JavaScript syntax and the local regression suite pass. **The complete browser journey remains unverified**, and this branch has no hosted product runtime. A founder test with a fresh partner requires both a deployable private runtime and working SerpApi quota; an archived First Lady artifact can be used for engineering replay but is historical evidence and cannot stand in for that partner's live investigation.

A later dashboard handoff review found that applying the research profile before the final preview assignments replaced its findings with preview findings. The bridge now applies last, clears preview scores, owner identity, booking platform and numeric confidence from the research profile, and the Missions navigation creates the research mission state before opening it. Mission progress is scoped to the operator website and claim, and its saved record retains the website. This repairs a real cross-mode truth leak; the focused bridge and claim ledger regressions pass. It does not substitute for a complete browser run.

The research dashboard also reads a saved `RESEARCH_PREPARED` mission only when both operator website and claim ID match. It changes the primary call to review the saved investigation and explicitly says approval and measurement remain ahead. A focused two-operator regression prevents saved state from being shown for the wrong business. This is local browser storage continuity, not a durable customer account, executed work or an outcome measurement.

The founder-selected next evaluation target is `https://truckeeriverraft.com/`. The private evaluation runner now accepts a single public HTTPS URL from a private issue or manual dispatch without modifying the standing corpus or adding a target-specific product rule. No fresh GO judgment has been made for this target. SerpApi quota was exhausted on the last live attempt; inspect remaining searches and renewal date before purchasing or triggering a paid investigation. A fresh result must be inspected before any founder-facing dashboard test.

Browser verification remains an explicit release gate. The available cloud browser rejects local `file://` paths by policy, and this branch has no accessible GO staging URL. Do not route around that restriction; establish a legitimate staged product runtime, then check the dashboard-to-Mission journey there. The private Actions research runner is not itself a hosted dashboard or a durable customer service.

### Parallel product slice: record an outcome honestly

After re-reading the company and product direction and inspecting existing Mission and work code, Builder chose one no-provider-cost vertical slice: a researched Mission can now record an actual baseline, an operator-reported approved action, and later same-period observations. The dashboard displays the saved observation for the matching researched claim and explicitly withholds causal or revenue attribution. This extends the real opportunity → Mission → action record → measurement → memory chain. It does not invoke an external system, alter the research pipeline or populate the founder test with sample numbers. Focused state/isolation and dashboard regressions pass with the full local suite.

The existing `GOWorkEngine` default journal, approval, monitoring and revenue model are synthetic preview material; wiring them directly to a researched operator would make the product untrustworthy. The new Outcome record instead requires operator input and stays in local browser storage. The UI journey, including this new panel, remains **unverified in a browser** until a legitimate staging URL exists. Do not use Markus as mechanical QA. A durable account-scoped work record and an approved, verifiable first execution capability are the strongest next company/product move after the Truckee checkpoint, followed by connected baseline and outcome data.
