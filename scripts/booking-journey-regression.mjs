import fs from 'node:fs';import vm from 'node:vm';const box={window:{}};vm.createContext(box);vm.runInContext(fs.readFileSync(new URL('../js/booking-journey-intelligence.js',import.meta.url),'utf8'),box);const build=box.window.GOBookingJourney.build;
const full=build({acquisition:{pages:[{markdown:'# Private Tour\nFrom $129\nBook Now\nBOOKING LINK: https://fareharbor.com/example'}]},dossier:{products:[{name:'Private Tour',urls:['/private']}],booking:{provider:'FareHarbor',status:'OBSERVED'}}});
const thin=build({acquisition:{pages:[{markdown:'# Private Tour\nCall us to learn more'}]},dossier:{products:[{name:'Private Tour'}],booking:{status:'UNKNOWN'}}});
const cases=[['positive path observed',full.state,'BOOKING_PATH_OBSERVED'],['provider retained',full.provider,'FareHarbor'],['thin extraction stays limited',thin.state,'LIMITED_PUBLIC_PATH'],['thin extraction not called conversion defect',/Do not call this a conversion problem/.test(thin.action),true]];
let fail=0;for(const [n,a,e] of cases){if(a!==e){fail++;console.error('FAIL '+n+': '+a+' != '+e)}else console.log('PASS '+n)}if(fail)process.exit(1);console.log('\n'+cases.length+'/'+cases.length+' booking-journey checks passed');